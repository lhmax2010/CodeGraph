# Stage 06 - E2E Search/Definition / Progress
> 开发过程持续追加，记录思考与决策，而非仅结果。

## 关键决策
- 决策：P6 风险档定为高风险。
  - 原因：这是 P1-P5 首次真实集成，主要风险来自 clangd/P5 索引、P4 helper、P2 路由契约之间的接缝。
  - 排除的方案：不把 P6 当作单模块实现阶段处理。
- 决策：P6 先接链路跑真实查询，再针对性处理 P6 前遗留项。
  - 原因：P6 的核心价值是暴露集成问题；预防性返工 P3/P4 容易扩大范围并掩盖真实接缝。
  - 排除的方案：开工前逐个预防性修完 `diagnostics_wait`、候选过度采集、宏体近似。
- 决策：P6 必须显式处理 clangd background-index 模式。
  - 原因：当前 `ClangdAdapter` 复用的 `LSPClient` 默认带 `--background-index=false`，与 P5 result 里 2 refs vs 389 refs 的证据一致；真实跨文件查询需要消费 P5 全局索引。
  - 排除的方案：沿用单 TU 模式冒充端到端。
- 决策：P6 API 在 background-index readiness 未确认时，把本次查询的 `index_health` 降为 `unknown`。
  - 原因：clangd background-index 异步加载；若空结果发生在索引未 ready 阶段，继续传 P5 `complete` 会让 P2 可能走 not_found，形成虚假否定。
  - 排除的方案：只看 P5 分片健康就允许空结果进入 not_found 分支。
- 决策：background-index ready 必须由查询无关的 sentinel 证明，不能用用户查询 symbol 非空作为 ready 判据。
  - 原因：用户 symbol 可能在单 TU/include header 中立即可见，误触发 ready 后 `get_definition` 会不稳定地停在 `.h` 声明，未等到全局 `.c` 实现。
  - 排除的方案：继续用 `search_symbol(query_symbol)` 非空作为 ready；固定 sleep 后不验证全局索引。
- 决策：`background_index=False` 的 API 查询一律视为未消费全局索引，传给 P2 的 `index_health` 降为 `unknown`、`index_scope` 降为 `current_tu`。
  - 原因：bg=False 是 P3 单 TU 可预测模式，即使 P5 分片健康也不能据此做本次查询的项目级负证明。
  - 排除的方案：沿用 P5 `complete` health 让空结果进入 not_found。

## 改动摘要
- 文件/模块：
  - `.dev_memory/INDEX.md`：登记 stage06 进行中。
  - `.dev_memory/stage05_index_build/result.md`：校正 stage05 已 Merge 事实。
  - `.dev_memory/stage06_e2e/plan.md`：记录 P6 计划、边界、集成问题。
  - `tools/verify_clangd.py`：对复用资产做最小修改，为 `LSPClient` 增加 `background_index` 可选参数，默认仍为 `False`。
  - `codegraph/engines/clangd_adapter.py`：`ClangdAdapterConfig` 增加 `background_index` 开关，默认 `False`，并新增 `warm_file()` 供 P6 触发 clangd 加载 CDB/索引。
  - `codegraph/api.py`：新增 P6 库 API 薄层，组装 P3/P4/P5 后调用 P2 routing。
  - `tests/test_api.py`：新增 P6 API 端到端/防虚假 not_found/非法输入/engine failure/精确过滤测试。
  - `codegraph/api.py`：修复 review BLOCKER：bg=False 不再携带 P5 complete 做负证明；background-index ready 改为配置化 sentinel（symbol + path suffix）证明；`search_symbol` 改为先过量拉取再精确过滤/分页，避免 fuzzy 命中挤掉精确结果。
  - `tests/test_api.py`：补 blocker 回归：bg=False 空结果必须 UNRESOLVED/unknown；ready 不被 query symbol/header 命中误触发；suffix 不匹配不标 complete；engine failure/非法输入/module registry 分支。

## 进度日志
- 2026-07-01 从 `main` 开 `phase/6-e2e-search-def`，baseline `7717257 [Phase 5] docs: record index build checkpoint`。
- 2026-07-01 baseline：`PYTHONPATH=.:tools .venv/bin/python -m pytest tests/ -q` -> `113 passed in 1.99s`。
- 2026-07-01 探测：`/home/linhao/Toolchain/codes/rw_arm/.cache/clangd/index` 存在 3593 个 `.idx`；P5 验收临时 `/tmp/codegraph-p5-arm-index-20260624-154443` 已不可用，P6 可复用现有真实分片。
- 2026-07-01 P3 回归：`PYTHONPATH=.:tools .venv/bin/python -m pytest tests/test_clangd_adapter.py -q` -> `11 passed in 0.12s`。默认单 TU 模式保持不变，新增显式 background-index 开关测试。
- 2026-07-01 真实 ARM 实测（复用 vs 重建）：启动 `clangd --background-index=true`，cwd/compile dir 指向 `/home/linhao/Toolchain/codes/rw_arm`，打开真实 TU 后未触发 31s 重建；分片前后均为 3593 个 `.idx`、总大小 39911040 bytes、最新 mtime ns `1781072784911021003`，确认本次查询复用现有分片。
- 2026-07-01 真实 ARM 实测（索引 ready 时机）：只 initialize 不 open TU 时，`workspace/symbol(gst_element_set_state)` 轮询 12s 仍为 0；打开 `gst-launch.c` 后，`workspace/symbol` 在约 1.213s 返回 `gstelement.c` 命中，`find_references` 在约 5.266s 复现 389 refs。结论：P6 必须先 open/warm 一个 TU，再等待/轮询目标符号确认索引 ready。
- 2026-07-01 真实 ARM 实测（P6 API）：`CodeGraph(BuildConfig("arm", "/home/linhao/Toolchain/codes/rw_arm", background_index=True))` 下，`search_symbol("gst_element_set_state")` -> 1.638s，`status=OK`，`index_health=complete`，语义结果为 `gstelement.c:2951`；`get_definition` 从 `gst-launch.c:565` 调用点 -> 0.836s，`status=OK`，`index_health=complete`，语义结果为 `gstelement.h:1153`。`get_definition` 在该 C 工程里返回声明位置，单 TU/全局模式无差异；全局索引消费的实证来自 `search_symbol` 返回 source `.c` 定义与 P7 留底的 389 refs 对照。
- 2026-07-01 真实 ARM 补测（get_definition 全局索引对照）：扩大扫描 180 个真实 CDB 源文件/1702 个调用探针，先 warm 到 `workspace/symbol(gst_element_set_state)` ready（约 1 个 0.5s poll），再比较 `background_index=False` 与 `True`。确认 `get_definition` 自身会因全局索引从头文件声明跳到跨文件 `.c` 实现：例如 `gstbufferlist.c:91` 的 `gst_buffer_ref`，单 TU -> `gstbuffer.h:463`，全局 -> `gstbuffer.c:3014`；`gstbufferlist.c:35` 的 `gst_pad_push_list`，单 TU -> `gstpad.h:1515`，全局 -> `gstpad.c:4920`；`gstcontext.c:35` 的 `gst_element_set_context`，单 TU -> `gstelement.h:1016`，全局 -> `gstelement.c:3604`。本轮前 53 个有效探针即发现 10 个 `.c` 实现级差异，坐实 `get_definition` 代码路径已消费 P5 background-index；此前 `gst_element_set_state` 无差异只是该符号/位置恰好单 TU 已能解析到声明。
- 2026-07-01 P6 定向：`PYTHONPATH=.:tools .venv/bin/python -m pytest tests/test_api.py -q` -> `5 passed in 0.39s`；`tests/test_clangd_adapter.py` 已回归 `11 passed`。
- 2026-07-01 全量 gate：`PYTHONPATH=.:tools .venv/bin/python -m pytest tests/ -q` -> `119 passed in 1.88s`；coverage -> `119 passed`，total 92%，`codegraph/api.py` 83%，`clangd_adapter.py` 100%；`.venv/bin/ruff check .` -> All checks passed；`.venv/bin/black --check .` -> 22 files unchanged；`.venv/bin/mypy codegraph` -> Success；`.venv/bin/python -m compileall -q codegraph tools tests` -> 通过。
- 2026-07-01 Review BLOCKER 修复真实回归：`gstbufferlist.c:91` 的 `gst_buffer_ref`，`background_index=False` 下 `search_symbol` -> `UNRESOLVED/index_unknown`（不再 not_found），`get_definition` -> `UNRESOLVED/index_unknown`；`background_index=True` 且 sentinel=`gst_buffer_ref`/suffix=`gstbuffer.c` 下，`search_symbol` 1.577s -> `OK/complete`，语义结果 `gstbuffer.c:3014`，`get_definition` 1.576s -> `OK/complete`，语义结果 `gstbuffer.c:3014`。
- 2026-07-01 BLOCKER 修复后 gate：`PYTHONPATH=.:tools .venv/bin/python -m pytest tests/test_api.py -q` -> `10 passed in 0.41s`；`PYTHONPATH=.:tools .venv/bin/python -m pytest tests/ -q` -> `124 passed in 1.89s`；coverage -> `124 passed`，total 91%，`codegraph/api.py` 82%，`clangd_adapter.py` 100%；`.venv/bin/ruff check .` -> All checks passed；`.venv/bin/black --check .` -> 22 files unchanged；`.venv/bin/mypy codegraph` -> Success；`.venv/bin/python -m compileall -q codegraph tools tests` -> 通过。
- 2026-07-01 Claude 独立复核：聚焦 P6 BLOCKER 修复的 `codegraph/api.py`/`tests/test_api.py` 摘录，结论 `NO BLOCKER/MAJOR`；确认 bg=False 降级、sentinel ready、exact-before-pagination 三项修复充分。Claude 提的 MINOR 死参 `_warm_background_index(symbol)` 已清理；两个 NIT（ready 后重复读 health、极端 fuzzy 排序超过 100 窗口）登记在 `docs/review/phase_6_review_result.md`，不阻塞本轮安全修复。
- 2026-07-01 Review 后最终 gate：`PYTHONPATH=.:tools .venv/bin/python -m pytest tests/test_api.py -q` -> `10 passed in 0.50s`；`PYTHONPATH=.:tools .venv/bin/python -m pytest tests/ -q` -> `124 passed in 1.94s`；coverage -> `124 passed`，total 91%，`codegraph/api.py` 82%，`clangd_adapter.py` 100%；`.venv/bin/ruff check .` -> All checks passed；`.venv/bin/black --check .` -> 22 files unchanged；`.venv/bin/mypy codegraph` -> Success；`.venv/bin/python -m compileall -q codegraph tools tests` -> 通过。
- 2026-07-02 P6 真机验收诊断（不改代码）：上一轮验收中 `gst_buffer_ref` 首次 bg=True `get_definition` 用时 12.823s 且 `UNRESOLVED/unknown`，后续 4 次约 1.57s 且稳定到 `gstbuffer.c:3014`。手工拆分当前热态单次路径：clangd init 0.034s、`warm_file` 0.001s、sentinel 轮询 6 次共 1.313s、`get_definition` LSP 0.010s、总计 1.431s。故意配置错误 suffix 使 ready 永不满足时，完整 P6 路径为 provider 0.000s、`index_health` 0.079s、engine init 0.032s、`_warm_background_index` 8.074s、definition 0.011s、route 0.008s、总计 8.278s。结论：`index_ready_timeout=8s` 不是硬 wall-clock 上限，因为每次 `workspace/symbol` 请求耗时计入循环，且循环只在发请求前检查 deadline；上一轮 12.8s 的额外约 4.5s 最可能来自 cold-cache 状态下某次 `workspace/symbol` 请求本身阻塞，而不是 `warm_file`、definition、P2 routing 或 tree-sitter。
- 2026-07-02 P6 真机验收诊断（进程/缓存）：用 tracing adapter 连续跑 3 次 `gst_buffer_ref` bg=True `get_definition`，clangd PID 分别为 1002389/1002424/1002459，说明当前 `CodeGraph` 每次查询都会新起 clangd 进程，没有复用长活进程。三次仍均约 1.57s 命中 `gstbuffer.c:3014`，因此 run2-5 变快不是进程复用，而是 P5 `.idx` / 文件系统 page cache / clangd 读取路径已被 run1 焐热。
- 2026-07-02 P6 真机验收诊断（顺序）：在当前热态下调换顺序三轮（buffer→pad→context、pad→context→buffer、context→buffer→pad），每轮第一个符号也都约 1.58s 命中 `.c`：`gst_buffer_ref -> gstbuffer.c:3014`、`gst_pad_push_list -> gstpad.c:4920`、`gst_element_set_context -> gstelement.c:3604`。这排除了“`gst_buffer_ref` 固定特殊、热态也必 miss”的解释；但因 OS cache 已热，未能证明 cold-cache 下“任意第一个符号都会 miss”。无 `vmtouch/hyperfine` 可无侵入驱逐缓存，本轮未执行 drop_caches。
- 2026-07-02 冷态严格验证权限：尝试 `sudo -n true`，返回 `sudo: a password is required`；当前 UID=1000，无法无交互执行 `sync && echo 3 > /proc/sys/vm/drop_caches`。因此未做真冷态 drop_caches 实验；当前判断仍基于 2026-07-01 首次验收的 run1 miss + run2-5 热态稳定，以及 2026-07-02 的分段/进程/顺序诊断。
- 2026-07-02 决策：P6 预热采用“焐 cache 不跳 warm”语义。`CodeGraph.prewarm()` / `prewarm_build_config()` 只提前执行 `warm_file + sentinel + path_suffix` 来焐热 OS page cache / `.idx` 读取路径；后续用户查询仍必须每次执行 `_warm_background_index` 证明“本次新起 clangd”已 ready。原因：诊断证明每次查询新起 clangd，预热 ready 不等于后续查询进程 ready；若跳过 warm，会重新引入 header 命中冒充 ready 的风险。预热失败不废掉 config，后续查询仍自行 warm，未 ready 仍降 `unknown/current_tu`。
- 2026-07-02 实现：新增显式 `CodeGraph.prewarm()`、`prewarm_build_config()`、`register_build_config(..., prewarm=True)`；不放进 `CodeGraph.__init__`，避免每次查询构造时重复预热。`_warm_background_index` 改为给 sentinel `workspace/symbol` 传剩余 wall-clock timeout，并在 `TimeoutError` 时返回 not-ready；预热和用户查询共用同一受控等待逻辑。`ClangdAdapter.search_symbol()` / `EngineObservation` 增加可选 `request_timeout`。
- 2026-07-02 预热后真机实测：在现有权限无法 drop_caches 的条件下，使用真实 `rw_arm` / 3593 `.idx` 跑 `CodeGraph(config).prewarm()` 后立即执行第一条用户查询 `get_definition(gst_buffer_ref @ gstbufferlist.c:91)`。本次预热耗时 13.048s 且返回 False（cold-ish sentinel 仍超时，但焐热了读取路径），紧接着第一条用户查询 1.600s -> `OK/complete`，语义结果 `gstbuffer.c:3014`；分片前后均为 `(3593, 39911040, 1781072784911021003)`，未重建。随后热态分段复测 prewarm 本身 3 次均 1.408s total、ready=True。结论：显式预热即使未在 cold-ish 首次拿到 ready，也能把用户首查从 12.8s miss 改为约 1.6s `.c` 命中；后续仍需最终真机验收确认。
- 2026-07-02 预热/timeout gate：`PYTHONPATH=.:tools .venv/bin/python -m pytest tests/test_api.py -q` -> `14 passed in 1.42s`；`PYTHONPATH=.:tools .venv/bin/python -m pytest tests/ -q` -> `128 passed in 4.36s`；coverage -> `128 passed`，total 93%，`codegraph/api.py` 92%，`clangd_adapter.py` 100%；`.venv/bin/ruff check .` -> All checks passed；`.venv/bin/black --check .` -> 22 files unchanged；`.venv/bin/mypy codegraph` -> Success；`.venv/bin/python -m compileall -q codegraph tools tests` -> 通过；`git diff --check` -> 通过。
- 2026-07-02 预热 timeout 语义补验：热态下分别用 `index_ready_timeout=8/12/20/30s` 跑真实 `gst_buffer_ref` prewarm，均约 1.407-1.418s 且 ready=True；因无法 drop_caches，不能证明 cold-cache 下 8s 必然不够，也不能证明 20/30s 必然 ready。故意 wrong suffix 让用户查询 not-ready：`get_definition(gst_buffer_ref)` 总耗时 8.198s，`OK/unknown -> gstbuffer.c:3014`，说明普通用户查询未 ready 时受 8s 等待窗口控制，没有复现 prewarm cold-ish 那次 13s 的 close 额外耗时；wrong-suffix prewarm 热态为 8.096s ready=False。结论：用户查询等待可控；独立更长 prewarm timeout 主要是启动阶段语义/冷态余量优化，而非查询安全必需。
- 2026-07-02 最终预热 timeout 小调：`BuildConfig.prewarm_index_ready_timeout: float | None = None`，`None` 时采用 `max(index_ready_timeout, 30.0)`；只影响 `CodeGraph.prewarm()` / `prewarm_build_config()`，用户查询 `_warm_background_index` 仍使用 `index_ready_timeout`。真实 `rw_arm` 复测：effective prewarm timeout=30.0，prewarm 1.408s ready=True，随后第一条用户查询 `get_definition(gst_buffer_ref @ gstbufferlist.c:91)` 1.581s -> `OK/complete`，语义结果 `gstbuffer.c:3014`；分片前后均为 `(3593, 39911040, 1781072784911021003)`，未重建。
- 2026-07-02 Claude 复核预热/timeout 行为修改：首轮发现一个属实安全洞（配置 `index_ready_probe_symbol` 但缺 `index_ready_probe_path_suffix` 时会把单 TU/header 命中误判为 ready）和一个属实分页问题（exact 命中总数与当前页混用，空页可能进入 `not_found`）。处理：缺 suffix 直接 not-ready 且不轮询；`search_symbol` exact page 与 total 分离；router 在 `total_hits>0` 的空页上禁止 not_found；`register_build_config` 返回值恢复 `None`；`ClangdAdapterConfig` 恢复旧位置参数顺序。
- 2026-07-02 Claude 聚焦复核 follow-up diff：结论无 BLOCKER/MAJOR，确认上述四项修复均朝 fail-safe 方向收敛。按其 MINOR 建议补了 `ClangdAdapterConfig` 运行时校验（防 `ClangdAdapterConfig(dir, path, True)` 静默误绑到 `extra_args`）、grep 确认仓库无位置参数 `background_index` 调用、补 `get_definition` ready+complete+空结果仍可达 `not_found` 的回归测试，以及 `_index_ready_probe_matches` 缺 suffix 的直接测试。
- 2026-07-02 最终 gate：`PYTHONPATH=.:tools .venv/bin/python -m pytest tests/ -q` -> `138 passed in 2.05s`；coverage -> `138 passed`，total `93%`，`codegraph/api.py` `93%`，`codegraph/engines/clangd_adapter.py` `100%`；`.venv/bin/ruff check .`、`.venv/bin/black --check .`、`.venv/bin/mypy codegraph`、`.venv/bin/python -m compileall -q codegraph tools tests`、`git diff --check` 全绿。
- 2026-07-02 four-way review 后续 BLOCKER 修复：Codex 抓到 `search_symbol` 的 `total_hits` 只是 `engine_limit=max(100, limit+offset)` 窗口内 exact 计数，若前 100 条全是 fuzzy、exact 在第 101 条之外，会得到 `total_hits=0` 并在 ready+complete 下误判 `not_found`。选择方案 A：语义窗口可能截断（返回数 `>= engine_limit`）且窗口内 exact 为 0 时，P6 API 层把本次负证明降为 not-ready，传给 P2 的 health/scope 变为 `unknown/current_tu`，只返回 `UNRESOLVED`，不再改 P2 状态机。
- 2026-07-02 环境修复：`.venv/bin/python` symlink 指向旧的 VS Code snap `code/245` uv Python，target 已不存在；按当前 uv Python 列表改回同版本 `3.11.15` 的 `code/248` 路径，未改项目依赖。随后 gate 使用 `.venv/bin/python` 正常运行。
- 2026-07-02 BLOCKER 修复 gate：`tests/test_api.py` -> `23 passed`，`tests/test_phase2_routing.py` -> `14 passed`，`tests/test_clangd_adapter.py` -> `13 passed`；全套 `PYTHONPATH=.:tools .venv/bin/python -m pytest tests/ -q` -> `139 passed in 3.70s`；coverage -> `139 passed`，total `93%`，`codegraph/api.py` `93%`，`codegraph/engines/clangd_adapter.py` `100%`；ruff/black/mypy/compileall/diff-check 全绿。
- 2026-07-07 four-way review 第四层 BLOCKER 修复：Codex 复现 `kind_filter=None` 被 API 默认成 `ORDINARY_FUNCTION`，而 `ORDINARY_FUNCTION` 在 P2 `_EXHAUSTIVE_KINDS` 内，导致“不带 kind filter 的任意符号查询”可在 ready+complete+空结果下断言 `not_found`。这会把“没有普通函数 foo”误报成“任何 foo 都不存在”。处理：`search_symbol(kind_filter=None)` 传 `SymbolKind.UNKNOWN`，只有显式 `function/variable/type` filter 传可穷尽 kind；`get_definition` 无 kind filter，也改传 `UNKNOWN`，避免对宏/类型/变量位置的空 definition 做函数级负证明。
- 2026-07-07 第四层 BLOCKER gate：`tests/test_api.py` -> `25 passed`，`tests/test_phase2_routing.py` -> `14 passed`；全套 `PYTHONPATH=.:tools .venv/bin/python -m pytest tests/ -q` -> `141 passed in 2.36s`；coverage -> `141 passed`，total `93%`，`codegraph/api.py` `93%`，`codegraph/engines/clangd_adapter.py` `100%`；ruff/black/mypy/compileall/diff-check 全绿。该修复仍需用户异构 review，未进入真机验收。
